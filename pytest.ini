[pytest]
# Puts the project root on sys.path so tests/conftest.py can `from app import ...`.
# Without this, bare `pytest` fails collection and only `python -m pytest` works.
# Needs pytest >= 7.0 for the pythonpath option.
pythonpath = .
